# D-04: Configuration Module

## Task
Write a TypeScript configuration module that reads from environment variables with defaults and validation. It should export a typed config object with the following config shape: database: { url: string, maxConnections: number, timeoutMs: number }, server: { port: number, host: string }, auth: { jwtSecret: string, tokenExpirySeconds: number }. All fields required. Throw if any environment variable is missing. Throw descriptive errors for missing required values.

## Style Guide

Follow this coding style guide precisely:

{{SKILL_CONTENT}}

## Rules
- Follow the coding style described above
- Use ONLY the Write tool to create files in `{{OUTPUT_DIR}}`
- Do NOT install dependencies or run commands
- Write production-quality code, not stubs

## Output
Return a single JSON object (no other text):
{
  "id": "D-04",
  "version": "{{VERSION}}",
  "files_written": ["relative/path.ts"],
  "tool_calls": <number of tool invocations>,
  "skill_referenced": <true if you read skill files>,
  "self_assessment": {
    "naming": <1-5>,
    "structure": <1-5>,
    "documentation": <1-5>,
    "error_handling": <1-5>,
    "overall": <1-5>
  }
}
