---
"@codewatch/cli": minor
---

Add two audit rules over `@titan-design/code-graph` 0.6.0's new per-function metrics: `symbol-loc` (flags functions over 60 lines) and `symbol-nesting` (flags functions with block nesting deeper than 4). Bumps `@titan-design/code-graph` to 0.6.0.
