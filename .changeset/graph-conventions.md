---
"@codewatch/cli": minor
---

Add `graph conventions` (convention map of capability areas with cached LLM summaries, `--query` ranking), the `get_conventions` MCP tool, and read API 1.3.0 `getConventions`/`findConventions`, consuming @titan-design/code-graph 0.4.0.
